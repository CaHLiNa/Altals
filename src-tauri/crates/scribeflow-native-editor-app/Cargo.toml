[package]
name = "scribeflow-native-editor-app"
version = "0.1.0"
edition = "2021"

[dependencies]
scribeflow-editor-core = { path = "../scribeflow-editor-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
